workspace: docker

# List the valid sites for this workspace
sites:
  - local

# Show archiving locations
archive:
  mounts:
    allenby: "/"
    aro: "/"
    chime: "/data/chime/baseband/processed"
    canfar: "/arc/projects/chime_frb/data/canfar/processed"
    gbo: "/"
    kko: "/"
    hco: "/"

http:
  baseurls:
    configs: http://pipelines_api:8001/v2
    pipelines: http://pipelines_api:8001/v2
    schedules: http://pipelines_api:8001/v2
    buckets: http://buckets:8004
    results: http://results:8005

config:
  archive:
    results: true
    plots:
      methods: ["move", "delete"] # Defines the methods enabled by the workspace admin
      storage: "s3"
      permissions: List[Linux Groups]?
    products:
      methods: ["move", "delete"] # Can also be copy or delete
      storage: "posix"
  slack:
